[tool.pytest.ini_options]
pythonpath = ["backend", "langgraph_service"]
asyncio_mode = "auto"
testpaths = ["backend/tests", "langgraph_service/tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501", "B008"]
